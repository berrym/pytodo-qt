# v0.3.11 Web UI Complete Overhaul — Mobile Access Mega Release

**Status:** In Progress — Core implementation + security + mobile wizard complete, remaining items tracked below
**Goal:** Transform the web UI from a read-mostly prototype into a fully interactive, professional-grade mobile experience that delivers near-parity with the desktop app — the kind of experience modern users expect from any real productivity app on their phone or tablet.

**Core Principle:** Privacy-first, self-hosted, LAN-first (no cloud required), but architecturally ready for the 0.4.x server mode and eventual optional cloud access for users who want it.

---

## Implementation Status

### What's Done
- **API:** 30 REST endpoints (full CRUD, NLP parse, board, columns, tags, trash/restore, filters, sort, presets, devices, auth)
- **Frontend:** Vanilla JS SPA (~3500 lines), mobile-first CSS (~2100 lines)
- **PWA:** Service worker with IndexedDB offline cache, versioned cache busting (v22)
- **Navigation:** Hash routing, 4-tab bottom nav (Home, Add, Search, More), view mode toggle
- **List View:** Full item cards with priority borders, metadata, overdue highlighting, subtask badges, sort
- **Board View:** Kanban columns with cards, column dots for phone, WIP indicators, add buttons
- **Smart Add:** NLP parsing with entity chip preview, list selector
- **Item Detail:** All fields editable (text, priority, date, time, tags, column, recurrence, pomodoros, subtasks)
- **Search & Filter:** Cross-list search with priority/status/due/tag filter chips
- **List Management:** Sheet-based picker with create, rename (inline), delete (confirmation)
- **Polish:** Swipe gestures (complete/delete), pull to refresh, animations, haptics, toast with undo (5s), WCAG AA contrast
- **Safety:** Trash/restore API and UI, 120px swipe threshold, 7-day tombstone recovery
- **Offline:** IndexedDB cache, offline queue with badge and per-item management, sync status toasts
- **Accessibility:** Focus trapping in sheets, Escape-to-close, ARIA dialog roles, aria-live toasts, reduced motion, 44px+ touch targets
- **Security:** Per-device token auth (WebDeviceStore in SQLite), 6-digit PIN pairing, TLS always-on with local CA, HSTS, CSP (wss: only), rate limiting, token revocation
- **Real-time:** WebSocket push (replaces polling), 30s heartbeat, exponential backoff reconnect
- **Three-tier sort:** Desktop-parity sort with GET/PUT /api/sort, shared config
- **Long-press context menu:** `showContextMenu()` bottom sheet with sub-menus, haptic, focus trap
- **Kanban column management:** Layout presets, add/rename/delete/WIP limit via long-press header
- **Kanban drag-and-drop:** Pick-up-and-place for phone, direct drag for tablet/desktop, edge auto-scroll
- **Mobile Access Wizard:** 5-page stateful wizard (device list, method choice, quick/trusted steps, cert reconfigure), per-device tracking, forget/revoke, PWA install guidance

### Remaining Items (v0.3.11 Scope)

#### Critical — Release Blockers
- [ ] **Web undo/redo** — at minimum, undo accidental delete (item gone with no recovery is unshippable)
- [ ] **Web sort parity** — web sometimes displays different order than desktop despite shared config

#### High Priority — Core Interactions
- [ ] **Smart add quick action buttons** — priority selector, date picker, tag selector, recurrence picker below entity chips
- [ ] **Smart add column selector** — choose kanban column when adding from board view
- [ ] **Subtask collapsible toggle** — tap chevron to expand/collapse subtasks under parent

#### Medium Priority — Polish & UX
- [ ] **Overdue items pinned to top** — option to sticky overdue items above all others
- [ ] **Smart add entity chip editing** — tap chips to modify extracted dates/priority/tags with pickers
- [ ] **"All done!" empty state** — celebratory state when all items in a list are completed
- [ ] **List reorder** — drag handles to reorder lists in the list picker
- [ ] **List progress bars** — visual completion bar alongside text percentage
- [ ] **Search: list filter** — filter search results to specific list(s)

#### Lower Priority — Advanced
- [ ] **Touch ripple effect** on interactive elements (Material-style feedback)
- [ ] **Tablet split-view layout** — two-column: list sidebar + item detail pane
- [ ] **Comprehensive ARIA** — screen reader announcements for dynamic state changes
- [ ] **Offline conflict indicator** — visual indicator when sync conflicts occur
- [ ] **Background sync API** — automatic replay via browser Background Sync where supported
- [ ] **Offline NLP fallback** — basic client-side regex parsing when server unavailable

### Release Readiness
- [ ] **Version bump** — pyproject.toml, core/settings.py, pytodo-qt.spec, release.yml
- [ ] **Manual testing** — iPhone Safari, iPad Safari, Android Chrome, Android Firefox
- [ ] **Landscape/portrait testing** — both orientations on phone and tablet
- [ ] **Dark/light mode testing** — both themes on all target devices
- [ ] **Offline testing** — airplane mode, make changes, reconnect and verify sync
- [ ] **PWA install testing** — Add to Home Screen on iOS and Android
- [ ] **Large item count testing** — 100+ items performance
- [ ] **Screen reader testing** — VoiceOver (iOS), TalkBack (Android)
- [ ] **Packaging: AppImage** — Linux binary distribution
- [ ] **Packaging: DMG** — macOS disk image (app to DMG)
- [ ] **Packaging: Windows installer** — MSI or NSIS installer

---

## Architecture Decisions

### Keep What Works
- **aiohttp on qasync** — single-thread, no locks, proven pattern
- **Vanilla JS** — no build step, <50KB total, simple maintenance
- **Service worker + offline queue** — already implemented, extend it
- **WebSocket push** — real-time updates via WS broadcast, polling as fallback
- **Per-device token auth** — PIN pairing, device store in SQLite, per-device revocation

### New Architectural Choices
- **Server-side NLP parsing** — `nlp_parser.py` is pure Python, no Qt dependency, perfect for the API
- **Single-page app with client-side routing** — hash-based routes (`#/`, `#/search`, `#/settings`, `#/item/{id}`)
- **Component-based vanilla JS** — small render functions, no framework, but organized into logical components
- **CSS custom properties** — full dark/light theme via `prefers-color-scheme` media query
- **Touch-first interactions** — swipe, pull-to-refresh, long-press, all via pointer events
- **Responsive breakpoints** — phone (<600px), tablet (600-1024px), desktop (>1024px)
- **Data fingerprinting** — JSON comparison skips DOM re-render when poll data unchanged (eliminates flicker)
- **IndexedDB cache** — persistent offline storage for lists and items, more robust than localStorage

---

## Phase 1: API Expansion ✅

All endpoints implemented and tested. See `web/api.py`.

### Endpoints
```
GET    /api/lists                                All non-private lists with counts
GET    /api/lists/{list_id}?q=...&tags=...       Single list with filtered items
POST   /api/lists                                Create list
PUT    /api/lists/{list_id}                      Rename list
DELETE /api/lists/{list_id}                      Soft-delete list
GET    /api/lists/{list_id}/board                Items grouped by kanban column
PATCH  /api/lists/{list_id}/columns              Add/remove/rename/reorder/set_wip_limit
POST   /api/lists/{list_id}/items                Create item (with parse_nlp option)
PUT    /api/items/{item_id}                      Update all mutable fields
DELETE /api/items/{item_id}                      Soft-delete item
PATCH  /api/items/{item_id}/toggle               Toggle completion
PATCH  /api/items/{item_id}/move                 Move to kanban column
PATCH  /api/items/{item_id}/parent               Set/clear parent (subtask)
PATCH  /api/items/{item_id}/restore              Un-delete (restore from trash)
POST   /api/parse                                NLP text parsing
GET    /api/tags                                 All tags across non-private lists
GET    /api/trash                                Recently deleted items (7-day window)
GET    /api/status                               App version, counts
```

---

## Phase 2: Frontend — Core Navigation & Layout ✅

All items implemented.

### Bottom Navigation Bar
Four tabs, fixed at bottom (standard mobile pattern):
- **Home** — list selector + items view (list or board mode, user's choice)
- **Add** (+) — floating action button center tab, opens smart add sheet
- **Search** — search/filter view
- **More** — settings, about, connection status, trash, offline queue (with pending badge)

### View Mode Toggle (List ↔ Board)
Matches the desktop app exactly: list and board are **two view modes of the same screen**, not separate navigation destinations. The user toggles between them in-place while staying on the same list.

### View Structure (Hash Routing)
```
#/                  → Home view (list or board mode, user's choice)
#/search            → Search/filter
#/item/{id}         → Item detail sheet
#/settings          → Settings/status
```

### Header
- Dynamic title based on current view
- List picker button (opens list management sheet)
- View mode toggle — segmented List|Board button (only shown on Home view)
- Connection status indicator (green dot = connected, amber = offline)

---

## Phase 3: Frontend — List View Overhaul ✅ (with remaining items)

### Implemented
- Priority indicator (colored left border: red/blue/gray)
- Large checkbox (24px) with bounce animation on toggle
- Item text with strikethrough when complete
- Metadata row: due date (overdue/today/upcoming coloring), tags as chips, recurrence icon, missed badge, pomodoro progress, subtask badge
- Subtask display (indented, left border accent)
- Swipe right → Complete (green, checkmark), Swipe left → Delete (red, ×) — 120px threshold
- Sort button (default, priority, due date, alphabetical, created)
- Overdue highlighting with relative text ("Overdue (3d)", "Today", "Tomorrow")
- Pull to refresh with spinner
- Empty state ("No items yet — tap + to add your first task")
- WCAG AA contrast for completed items (no opacity overlay)

### Remaining
- Long-press → Context menu (edit, move to column, add subtask, set priority)
- Collapsible subtasks (tap chevron to expand/collapse)
- Sort direction toggle (asc/desc)
- Overdue items pinned to top option
- "All done!" celebratory empty state

---

## Phase 4: Frontend — Smart Add Experience ✅ (with remaining items)

### Implemented
- Smart input sheet (slides up from bottom)
- Large text input with server-side NLP preview (debounced 300ms)
- Entity chips displayed below input (date, time, priority, tag, recurrence, pomodoro — color-coded)
- List selector (defaults to current list)
- Offline queue support for adds

### Remaining
- Tap entity chips to edit with native pickers
- Quick action buttons below chips (priority !/!!/!!!, date picker, tag selector, recurrence picker)
- Column selector (which kanban column, when in board view)
- Client-side NLP fallback when offline

---

## Phase 5: Frontend — Item Detail / Edit Sheet ✅

All core items implemented.

- Editable item text (textarea)
- Status toggle (complete/incomplete with visual indicator)
- Priority selector (3-button High/Normal/Low with color coding)
- Due Date (native date picker)
- Due Time (native time picker)
- Tags (text input, comma-separated)
- Kanban Column (dropdown of list's board_columns)
- Recurrence (type selector + interval + end conditions)
- Pomodoro Estimate (number input)
- Pomodoro Progress (read-only: sessions and minutes)
- Subtasks (list of children with inline add)
- Metadata (created date, last modified)
- Delete button
- Auto-save (500ms debounce on field change)
- Visual "Saving..." → "Saved ✓" indicator

---

## Phase 6: Frontend — Kanban Board View ✅ (with remaining items)

### Implemented
- Full kanban board as alternate view mode within Home (segmented toggle)
- Phone: single column visible, horizontal scroll with snap, column indicator dots
- Tablet: 2-3 columns side by side
- Column header with name + item count + WIP limit indicator
- Cards: compact version with priority border, text, due date, tags
- Add card "+" button at bottom of each column

### Remaining
- Drag and drop: touch-based card dragging between columns
- Column management: long-press column header for rename/delete/set WIP limit
- Swipe column navigation gesture (phone)

---

## Phase 7: Frontend — Search & Filter ✅ (with remaining items)

### Implemented
- Sticky search input at top
- Real-time search as you type (server-side filtered, across all non-private lists)
- Filter options: Priority (High/Normal/Low), Status (All/Active/Completed), Due (Overdue/Today/Week/None), Tags (dynamic chips)
- Results grouped by list with section headers
- Same card rendering as list view
- Tap to open item detail sheet

### Remaining
- List filter (filter results to specific list(s))

---

## Phase 8: Frontend — List Management ✅ (with remaining items)

### Implemented
- List picker sheet (replaces old `<select>` dropdown)
- Shows all lists with item counts, completion %, overdue count
- Create new list (input + button)
- Rename list (inline edit)
- Delete list (with confirmation showing item count)
- Visual indicator for currently selected list

### Remaining
- Reorder lists (drag handle)
- Visual completion progress bar

---

## Phase 9: Frontend — Polish & Native Feel ✅ (with remaining items)

### Implemented
- View transitions: directional slide between nav views
- Sheet transitions: slide up/down with backdrop fade
- Item animations: fade+slide enter, slide-out exit for deletes
- Checkbox: scale bounce animation
- Swipe: smooth follow-finger with dampened movement
- Toast notifications with Undo button (5-second window for deletes)
- Haptic feedback on toggle, delete, swipe, pull-to-refresh, restore
- Dark/light mode via `prefers-color-scheme` with full theme variables
- System font stack with proper fallbacks
- Safe area insets for notched phones
- Accessibility: Escape-to-close sheets, focus trapping, ARIA dialog roles, aria-live toasts
- Reduced motion support (`prefers-reduced-motion`)
- Minimum 44px touch targets
- WCAG AA contrast compliance

### Remaining
- Touch ripple effect on interactive elements
- Tablet split-view layout (sidebar + detail)
- Comprehensive screen reader announcements for state changes

---

## Phase 10: Service Worker & Offline Enhancement ✅ (with remaining items)

### Implemented
- IndexedDB cache (pytodo_cache) with lists and per-list item stores
- Cache on every successful fetch, fall back to IndexedDB when offline
- Offline CRUD: add, toggle, delete queued with offline queue
- Pending changes badge (red count) on More nav tab
- Queue viewer in Settings: human-readable action descriptions, relative timestamps, per-item remove
- Clear all pending changes button with confirmation
- Sync progress toasts on reconnect
- Service worker: cache-first static, network-first API, versioned cache (v7)
- Pre-cache all static assets on install

### Remaining
- Conflict indicator when online data differs from offline edits
- Background sync API integration (automatic replay)
- Graceful handling of deleted-on-server items during offline queue replay
- Client-side NLP fallback when server unavailable

---

## File Structure

```
src/pytodo_qt/web/
├── __init__.py
├── server.py              (embedded aiohttp on qasync)
├── api.py                 (20 REST endpoints)
├── static/
│   ├── index.html         (app shell with bottom nav, sheets, search, settings)
│   ├── style.css          (~1640 lines, full mobile app styles with dark/light theme)
│   ├── app.js             (~1900 lines, SPA with routing, offline, IndexedDB)
│   ├── sw.js              (service worker with versioned cache, network strategies)
│   ├── manifest.json      (PWA manifest)
│   ├── icon-192.png
│   └── icon-512.png
```

---

## Testing

### Automated Tests (132 total)
- `test_web_api.py` — 94 tests (CRUD, toggle, parse, board, columns, parent, filters, trash/restore)
- `test_web_pwa.py` — 38 tests (static files, manifest, SW, offline queue, routes)

### Manual Testing Checklist
- [ ] iPhone Safari (portrait + landscape)
- [ ] iPad Safari (portrait + landscape)
- [ ] Android Chrome (portrait + landscape)
- [ ] Android Firefox
- [ ] Desktop browsers (Chrome, Firefox, Safari)
- [ ] Dark mode and light mode on all above
- [ ] Screen reader (VoiceOver, TalkBack)
- [ ] Offline mode (airplane mode → changes → reconnect)
- [ ] PWA install (Add to Home Screen) on iOS and Android
- [ ] Large item counts (100+ items)

---

## API Impact on Desktop

All API changes are additive. The desktop app is unaffected:
- New endpoints don't change existing ones
- `_item_to_json()` gains more fields (backward compatible)
- NLP parser is already imported in desktop code
- Board column operations already exist in desktop commands

---

## Relationship to Future Versions

### 0.3.12 — PySide6 Port
- Web UI code has ZERO Qt dependency in the frontend (pure HTML/JS/CSS)
- API handlers use `QTimer.singleShot` (trivially portable to PySide6)
- No changes needed to web UI for the port

### 0.4.x — Server Mode
- All API endpoints designed here become the server mode API
- Add authentication layer (sessions, tokens) on top of existing routes
- Add TLS termination
- Headless mode: just the web server without Qt GUI
- WebSocket upgrade for real-time sync (replace polling)

### 0.5.x+ — Native Mobile / Cloud
- PWA proves out mobile patterns and UX
- Same API serves native mobile apps
- Optional cloud relay for remote access (zero-knowledge encrypted)
- Push notifications via Web Push API or native push

**This web UI is not throwaway work.** Every API endpoint, every UX pattern, every interaction design carries forward. The frontend HTML/JS is the only part that might eventually be superseded by native apps, and even then the PWA remains as a universal fallback.

---

## Non-Goals for v0.3.11

- **Authentication/TLS** — deferred to 0.4.x (LAN trust model is fine for now)
- **Multi-user** — deferred to 0.4.x (server mode prerequisite)
- **Push notifications** — requires HTTPS/service worker push, deferred to 0.4.x+
- **WebSocket real-time sync** — polling is acceptable; WebSocket in 0.4.x
- **Native mobile apps** — this PWA IS the mobile app for now
- **Cloud relay** — 0.5.x+
- **Pomodoro timer control from mobile** — display only (timer runs on desktop); future consideration
- **JS framework migration** — vanilla JS serves us well at this scale
